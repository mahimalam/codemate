# Architecture

These documents describe the implementation that ships in this repository.

- [Agent harness](AGENT-HARNESS.md): runtime boundaries, provider routing, tools, recovery, permissions, and verification.
- [IDE design](UI-DESIGN.md): React workbench structure, components, visual system, and interaction behavior.

Run `npm run check` before submitting changes. It validates TypeScript, builds the production renderer, and runs the Python contract suite.
